# 🔗 Как открыть ссылки - РЕШЕНИЕ

## Проблема: Ссылки не открываются автоматически

## ✅ Решение: Открыть вручную

### Способ 1: Команда open (macOS)

**После запуска обоих сервисов**, выполните в новом терминале:

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform

# Открыть Strapi
open http://localhost:1337/admin

# Открыть Next.js
open http://localhost:3000
```

Или используйте готовый скрипт:
```bash
./open-links.sh
```

### Способ 2: Скопировать ссылку вручную

1. **Скопируйте эти ссылки:**
   - `http://localhost:1337/admin`
   - `http://localhost:3000`

2. **Вставьте в адресную строку браузера** (Safari, Chrome, Firefox)

3. **Нажмите Enter**

### Способ 3: Проверить, что сервисы работают

```bash
# Проверить Strapi
curl http://localhost:1337/api

# Если видите JSON ответ - Strapi работает
# Если ошибка - Strapi не запущен
```

```bash
# Проверить Next.js
curl http://localhost:3000

# Если видите HTML - Next.js работает
# Если ошибка - Next.js не запущен
```

## 🚀 Полный процесс запуска:

### 1. Запустить Strapi (Терминал 1)
```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/cms
npm run develop
```
Дождитесь: `[INFO] Strapi is running at http://localhost:1337`

### 2. Запустить Next.js (Терминал 2)
```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/website
npm run dev
```
Дождитесь: `- Local: http://localhost:3000`

### 3. Открыть ссылки (Терминал 3 или вручную)
```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform
./open-links.sh
```

Или вручную:
- Откройте браузер
- Введите: `http://localhost:1337/admin`
- Введите: `http://localhost:3000`

## ⚠️ Если ссылки все еще не работают:

1. **Убедитесь, что сервисы запущены** - в терминалах должны быть сообщения
2. **Проверьте порты:**
   ```bash
   lsof -i :1337
   lsof -i :3000
   ```
3. **Попробуйте другой браузер**
4. **Проверьте firewall** в настройках macOS

## ✅ Готово!

Ссылки должны открываться через команду `open` или вручную в браузере.




