# 🚀 Запуск сервисов - ИСПРАВЛЕНО

## ✅ Что исправлено:

1. ✅ Конфигурационные файлы Strapi переведены с `.ts` на `.js`
2. ✅ Добавлен `config/admin.js` с настройками аутентификации
3. ✅ Секретные ключи сгенерированы и настроены
4. ✅ Next.js конфигурация обновлена

## 🎯 Запуск сервисов:

### Терминал 1: Strapi CMS

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/cms
npm run develop
```

**Ожидаемый вывод:**
```
[INFO] Strapi is running at http://localhost:1337
```

**Откройте в браузере:** http://localhost:1337/admin

### Терминал 2: Next.js Website

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/website
npm run dev
```

**Ожидаемый вывод:**
```
- ready started server on 0.0.0.0:3000
- Local:        http://localhost:3000
```

**Откройте в браузере:** http://localhost:3000

## 🔍 Проверка работы:

После запуска обоих сервисов, выполните:

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform
./check-services.sh
```

Скрипт проверит:
- ✅ Работает ли PostgreSQL
- ✅ Запущены ли сервисы
- ✅ Отвечают ли они на HTTP запросы

## 📝 Если ссылки все еще не открываются:

1. **Проверьте терминалы** - должны быть видны сообщения о запуске
2. **Проверьте браузер** - попробуйте скопировать ссылку и вставить в адресную строку
3. **Проверьте firewall** - может блокировать локальные подключения
4. **Попробуйте другой браузер** или инкогнито режим

## 🐛 Если есть ошибки:

Смотрите логи в терминалах. Частые проблемы:

- **Strapi:** Проверьте `.env` файл, все ключи должны быть заполнены
- **Next.js:** Проверьте, что зависимости установлены (`npm install`)

## ✅ Готово к тестированию!

Запустите оба сервиса и откройте ссылки в браузере.





