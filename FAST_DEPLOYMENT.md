# ⚡ Быстрый деплой на Vercel

## ✅ Что уже настроено для быстрого деплоя

### 1. Оптимизированная конфигурация Vercel

**`vercel.json`:**
- ✅ `installCommand`: `npm install --prefer-offline --no-audit` - использует кэш и пропускает аудит
- ✅ `ignoreCommand`: проверяет изменения только в `apps/website/` - пропускает деплой если изменений нет
- ✅ `buildCommand`: оптимизирован для монорепо структуры

### 2. Кэширование зависимостей

Vercel автоматически кэширует:
- ✅ `node_modules` между деплоями
- ✅ `.next` build cache
- ✅ npm cache

### 3. Исключение ненужных файлов

**`.vercelignore`:**
- ✅ Исключены `node_modules`, `apps/cms`, `packages` и другие ненужные файлы
- ✅ Уменьшает размер загружаемых файлов

### 4. Next.js оптимизации

**`next.config.ts`:**
- ✅ `output: 'standalone'` - минимальный размер билда
- ✅ `compress: true` - сжатие ответов
- ✅ ISR (Incremental Static Regeneration) настроен для страниц
- ✅ Image optimization включена

## 🚀 Настройка автоматического деплоя из Git

### Вариант 1: Через Vercel Dashboard (Рекомендуется)

1. **Подключите Git репозиторий:**
   - Откройте [Vercel Dashboard](https://vercel.com/dashboard)
   - Выберите проект `website`
   - Перейдите в **Settings** → **Git**
   - Нажмите **Connect Git Repository**
   - Выберите ваш репозиторий (GitHub/GitLab/Bitbucket)

2. **Настройте автоматический деплой:**
   - **Production Branch**: `main` или `master`
   - **Root Directory**: `apps/website` (если деплоите из корня монорепо)
   - **Build Command**: `npm run build` (или оставьте по умолчанию)
   - **Output Directory**: `.next`
   - **Install Command**: `npm install --prefer-offline --no-audit`

3. **Настройте переменные окружения:**
   - **Settings** → **Environment Variables**
   - Добавьте:
     ```
     NEXT_PUBLIC_SITE_URL=https://nocko.com
     NEXT_PUBLIC_STRAPI_URL=https://your-strapi-url.com
     STRAPI_API_TOKEN=your_token
     ```

### Вариант 2: Через GitHub Actions (Опционально)

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy to Vercel

on:
  push:
    branches: [main]
    paths:
      - 'apps/website/**'
      - 'packages/**'
      - 'vercel.json'
      - '.vercelignore'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci --prefer-offline
      
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: apps/website
          scope: ${{ secrets.VERCEL_ORG_ID }}
```

## ⚡ Оптимизации для максимальной скорости

### 1. Используйте Vercel CLI для локального деплоя

```bash
# Быстрый деплой только измененных файлов
cd apps/website
npx vercel --prod

# Или с автоматическим подтверждением
npx vercel --prod --yes
```

### 2. Настройте Preview Deployments

Vercel автоматически создает preview для каждого PR:
- ✅ Проверка изменений перед мерджем
- ✅ Автоматические комментарии в PR
- ✅ Быстрая обратная связь

### 3. Используйте Edge Functions для критичных путей

Для очень быстрого ответа можно использовать Edge Functions:
```typescript
// app/api/edge/route.ts
export const runtime = 'edge';
export const revalidate = 0; // No caching
```

### 4. Оптимизируйте зависимости

**`apps/website/package.json`:**
- Используйте точные версии зависимостей
- Удалите неиспользуемые пакеты
- Используйте `npm ci` вместо `npm install` в CI/CD

## 📊 Время деплоя

**Типичное время деплоя:**
- **Установка зависимостей**: 30-60 секунд (с кэшем: 5-10 секунд)
- **Сборка Next.js**: 30-90 секунд
- **Деплой**: 10-30 секунд
- **Итого**: 1-3 минуты

**С оптимизациями:**
- **С кэшем зависимостей**: 1-2 минуты
- **Только изменения в коде**: 30-60 секунд
- **Preview деплой**: 20-40 секунд

## 🔍 Мониторинг скорости деплоя

1. **Vercel Dashboard** → **Deployments**
   - Проверьте время каждого деплоя
   - Найдите узкие места

2. **Build Logs:**
   - Проверьте, какие шаги занимают больше всего времени
   - Оптимизируйте медленные части

3. **Analytics:**
   - Vercel Analytics показывает производительность
   - Используйте для оптимизации

## ✅ Чеклист для быстрого деплоя

- [x] `.vercelignore` создан и настроен
- [x] `vercel.json` оптимизирован
- [x] `installCommand` использует кэш
- [x] `ignoreCommand` настроен для монорепо
- [ ] Git репозиторий подключен к Vercel
- [ ] Автоматический деплой настроен
- [ ] Переменные окружения настроены
- [ ] Preview deployments включены
- [ ] Кэширование зависимостей работает

## 🆘 Решение проблем

### Медленный деплой

1. **Проверьте размер проекта:**
   ```bash
   du -sh apps/website
   ```

2. **Проверьте `.vercelignore`:**
   - Убедитесь, что исключены ненужные файлы
   - Проверьте размер загружаемых файлов

3. **Оптимизируйте зависимости:**
   - Удалите неиспользуемые пакеты
   - Используйте `npm ci` вместо `npm install`

### Деплой не запускается автоматически

1. Проверьте настройки Git в Vercel Dashboard
2. Убедитесь, что ветка `main`/`master` подключена
3. Проверьте права доступа к репозиторию

### Долгая установка зависимостей

1. Используйте `npm ci --prefer-offline`
2. Проверьте, что `package-lock.json` закоммичен
3. Используйте Vercel's dependency cache

## 📚 Дополнительные ресурсы

- [Vercel Git Integration](https://vercel.com/docs/concepts/git)
- [Vercel Build Optimization](https://vercel.com/docs/concepts/builds)
- [Vercel Caching](https://vercel.com/docs/concepts/builds/caching)
- [Next.js Deployment](https://nextjs.org/docs/deployment)


