# 🔒 Закрытие дефолтного домена Vercel (nocko-4.vercel.app)

## ✅ Решение: Редирект на основной домен

Дефолтный домен `.vercel.app` нельзя полностью удалить в Vercel, но можно настроить автоматический редирект на основной домен `nocko.com`.

## 📋 Что было сделано

Добавлен редирект в `vercel.json`:

```json
{
  "redirects": [
    {
      "source": "/(.*)",
      "has": [
        {
          "type": "host",
          "value": "nocko-4.vercel.app"
        }
      ],
      "destination": "https://nocko.com/:1",
      "permanent": true
    }
  ]
}
```

**Как это работает:**
- Все запросы к `nocko-4.vercel.app` автоматически редиректятся на `nocko.com`
- Сохраняется путь (например, `nocko-4.vercel.app/services` → `nocko.com/services`)
- Используется постоянный редирект (301) для SEO

## 🚀 Применение изменений

После обновления `vercel.json`:

1. **Закоммитьте изменения:**
```bash
git add vercel.json
git commit -m "Add redirect from vercel.app domain to nocko.com"
git push
```

2. **Или задеплойте напрямую:**
```bash
vercel --prod
```

3. **Проверьте редирект:**
   - Откройте `https://nocko-4.vercel.app` в браузере
   - Должен произойти автоматический редирект на `https://nocko.com`

## 🔍 Альтернативные способы

### Способ 1: Через Vercel Dashboard (не рекомендуется)

1. Войдите в Vercel Dashboard
2. Settings → Domains
3. Найдите `nocko-4.vercel.app`
4. Нажмите "Remove" (но это может не сработать, так как дефолтный домен нельзя удалить)

**Примечание:** Дефолтный домен `.vercel.app` нельзя полностью удалить, поэтому редирект через `vercel.json` - лучший вариант.

### Способ 2: Через Next.js Middleware (более гибкий)

Создайте файл `apps/website/middleware.ts`:

```typescript
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  const hostname = request.headers.get('host') || '';
  
  // Редирект с vercel.app домена на основной домен
  if (hostname.includes('vercel.app') && !hostname.includes('nocko.com')) {
    const url = request.nextUrl.clone();
    url.hostname = 'nocko.com';
    url.protocol = 'https';
    return NextResponse.redirect(url, 301);
  }
  
  return NextResponse.next();
}

export const config = {
  matcher: [
    /*
     * Match all request paths except for the ones starting with:
     * - api (API routes)
     * - _next/static (static files)
     * - _next/image (image optimization files)
     * - favicon.ico (favicon file)
     */
    '/((?!api|_next/static|_next/image|favicon.ico).*)',
  ],
};
```

**Преимущества middleware:**
- Более гибкая логика редиректов
- Можно добавить дополнительные проверки
- Работает на уровне Next.js

## ✅ Проверка работы

После применения изменений проверьте:

1. **Редирект работает:**
```bash
curl -I https://nocko-4.vercel.app
# Должен вернуть: HTTP/1.1 301 Moved Permanently
# Location: https://nocko.com/
```

2. **Сохранение путей:**
```bash
curl -I https://nocko-4.vercel.app/services
# Должен вернуть: HTTP/1.1 301 Moved Permanently
# Location: https://nocko.com/services
```

3. **В браузере:**
   - Откройте `https://nocko-4.vercel.app`
   - Должен автоматически перенаправить на `https://nocko.com`

## 📝 Важные замечания

1. **Дефолтный домен нельзя удалить:** Vercel всегда создает домен `.vercel.app` для каждого проекта. Его нельзя полностью отключить, но можно настроить редирект.

2. **SEO:** Постоянный редирект (301) сообщает поисковым системам, что основной домен - `nocko.com`, что хорошо для SEO.

3. **Кэширование:** После настройки редиректа может потребоваться время для распространения изменений (обычно несколько минут).

4. **SSL:** Оба домена (`nocko-4.vercel.app` и `nocko.com`) будут иметь SSL сертификаты от Vercel автоматически.

## 🆘 Решение проблем

### Редирект не работает

- Проверьте, что изменения задеплоены на Vercel
- Убедитесь, что `vercel.json` находится в корне проекта
- Проверьте логи деплоя в Vercel Dashboard

### Бесконечный редирект

- Убедитесь, что в `has.value` указан правильный домен
- Проверьте, что `destination` указывает на правильный домен

### Редирект работает, но медленно

- Это нормально, редирект добавляет один дополнительный запрос
- Для лучшей производительности используйте основной домен `nocko.com`

## 📚 Дополнительные ресурсы

- [Vercel Redirects Documentation](https://vercel.com/docs/concepts/projects/domains/redirects)
- [Next.js Middleware](https://nextjs.org/docs/app/building-your-application/routing/middleware)


