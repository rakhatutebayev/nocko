# 🔗 Как открыть ссылки

## Проблема: Ссылки не открываются в браузере

### Решение 1: Скопировать ссылку вручную

1. **Из терминала скопируйте ссылку:**
   - Strapi: `http://localhost:1337/admin`
   - Next.js: `http://localhost:3000`

2. **Вставьте в адресную строку браузера** и нажмите Enter

### Решение 2: Использовать команду open (macOS)

После запуска сервисов, в **новом терминале** выполните:

```bash
# Открыть Strapi
open http://localhost:1337/admin

# Открыть Next.js
open http://localhost:3000
```

### Решение 3: Проверить, что сервисы запущены

```bash
# Проверить статус
cd /Users/rakhat/Documents/webhosting/nocko-platform
./check-services.sh
```

### Решение 4: Попробовать другой браузер

- Safari
- Chrome
- Firefox
- Инкогнито режим

### Решение 5: Проверить firewall

macOS может блокировать localhost. Проверьте настройки:
- System Settings > Network > Firewall

## 🚀 Автоматическое открытие ссылок

Создайте скрипт для автоматического открытия:

```bash
# После запуска Strapi
sleep 10 && open http://localhost:1337/admin

# После запуска Next.js  
sleep 10 && open http://localhost:3000
```

## ✅ Проверка доступности

```bash
# Проверить Strapi
curl http://localhost:1337/api

# Проверить Next.js
curl http://localhost:3000
```

Если команды возвращают данные - сервисы работают, проблема в браузере.




