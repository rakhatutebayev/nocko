# 🚫 Блокировка индексации домена nocko-4.vercel.app

## ✅ Решение: X-Robots-Tag заголовок

Настроена полная блокировка индексации домена `nocko-4.vercel.app` поисковыми системами.

## 📋 Что было сделано

### 1. Добавлен заголовок в `vercel.json`

```json
{
  "headers": [
    {
      "source": "/(.*)",
      "has": [
        {
          "type": "host",
          "value": "nocko-4.vercel.app"
        }
      ],
      "headers": [
        {
          "key": "X-Robots-Tag",
          "value": "noindex, nofollow, noarchive, nosnippet"
        }
      ]
    }
  ]
}
```

### 2. Обновлен `middleware.ts`

Добавлена проверка домена и установка заголовка `X-Robots-Tag`:

```typescript
if (hostname.includes('vercel.app')) {
  response.headers.set('X-Robots-Tag', 'noindex, nofollow, noarchive, nosnippet');
}
```

## 🔍 Что означают директивы

| Директива | Описание |
|-----------|----------|
| `noindex` | Не индексировать страницы в поисковой выдаче |
| `nofollow` | Не переходить по ссылкам на странице |
| `noarchive` | Не сохранять копии страниц в кэше |
| `nosnippet` | Не показывать сниппеты в результатах поиска |

## 🚀 Применение изменений

После обновления файлов:

1. **Закоммитьте изменения:**
```bash
git add vercel.json apps/website/middleware.ts
git commit -m "Block indexing of vercel.app domain"
git push
```

2. **Задеплойте на Vercel:**
```bash
vercel --prod
```

3. **Проверьте заголовки:**
```bash
curl -I https://nocko-4.vercel.app
# Должен вернуть: X-Robots-Tag: noindex, nofollow, noarchive, nosnippet
```

## ✅ Проверка работы

### Проверка заголовков

```bash
# Проверить заголовки для vercel.app домена
curl -I https://nocko-4.vercel.app

# Ожидаемый результат:
# HTTP/1.1 301 Moved Permanently
# Location: https://nocko.com/
# X-Robots-Tag: noindex, nofollow, noarchive, nosnippet
```

### Проверка в браузере

1. Откройте `https://nocko-4.vercel.app` в браузере
2. Откройте DevTools → Network
3. Проверьте заголовки ответа
4. Должен быть заголовок `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet`

### Проверка через онлайн инструменты

- [Google Search Console](https://search.google.com/search-console) - проверьте, что vercel.app домен не индексируется
- [Bing Webmaster Tools](https://www.bing.com/webmasters) - аналогично для Bing
- [Header Checker](https://www.seoreviewtools.com/header-checker/) - проверьте заголовки

## 🔄 Как это работает

1. **Vercel.json заголовки:** Применяются на уровне Vercel для всех запросов к `nocko-4.vercel.app`
2. **Middleware заголовки:** Дополнительная проверка на уровне Next.js для всех vercel.app доменов
3. **Двойная защита:** Оба механизма работают вместе для максимальной надежности

## 📝 Важные замечания

1. **Редирект + блокировка:** Домен `nocko-4.vercel.app` будет:
   - Редиректить на `nocko.com` (301 редирект)
   - Иметь заголовок `X-Robots-Tag` для блокировки индексации

2. **Время применения:** Изменения вступят в силу после деплоя (обычно несколько минут)

3. **Существующие индексы:** Если домен уже проиндексирован:
   - Google может удалить его из индекса через несколько дней/недель
   - Можно ускорить через Google Search Console → Удаление URL

4. **Sitemap:** Убедитесь, что в `sitemap.xml` нет ссылок на `nocko-4.vercel.app`

## 🆘 Решение проблем

### Заголовок не устанавливается

- Проверьте, что изменения задеплоены на Vercel
- Убедитесь, что `vercel.json` находится в корне проекта
- Проверьте логи деплоя в Vercel Dashboard

### Поисковики все еще индексируют

- Подождите несколько дней/недель для обновления индекса
- Используйте Google Search Console для удаления уже проиндексированных страниц
- Проверьте, что заголовок действительно устанавливается (через curl или DevTools)

### Конфликт с другими заголовками

- Middleware устанавливает заголовок последним, поэтому он имеет приоритет
- Если есть конфликты, проверьте порядок применения в `vercel.json`

## 📚 Дополнительные ресурсы

- [Google: Blocking access to your site with robots.txt](https://developers.google.com/search/docs/crawling-indexing/robots/robots_meta_tag)
- [Vercel Headers Documentation](https://vercel.com/docs/concepts/projects/domains/headers)
- [Next.js Middleware](https://nextjs.org/docs/app/building-your-application/routing/middleware)

## ✅ Чеклист

- [ ] Изменения закоммичены и задеплоены
- [ ] Заголовок `X-Robots-Tag` проверен через curl
- [ ] Заголовок проверен в браузере (DevTools)
- [ ] Проверено в Google Search Console (если домен был проиндексирован)
- [ ] Убедились, что основной домен `nocko.com` индексируется нормально



