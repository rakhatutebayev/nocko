# 🧹 Чистый запуск после зависания

## ✅ Все зависшие процессы остановлены

## 🚀 Запуск заново (пошагово):

### Шаг 1: Очистка (уже сделано)

```bash
# Остановить все процессы
pkill -f "next dev"
pkill -f "strapi develop"

# Освободить порты
lsof -ti :3000 -ti :1337 | xargs kill -9
```

### Шаг 2: Запустить Strapi

**Терминал 1:**
```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/cms
npm run develop
```

**Дождитесь:**
```
[INFO] Strapi is running at http://localhost:1337
```

**Затем откройте:** http://localhost:1337/admin

### Шаг 3: Запустить Next.js

**Терминал 2 (новый терминал):**
```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform/apps/website
npm run dev
```

**Дождитесь:**
```
- Local:        http://localhost:3000
```

**Затем откройте:** http://localhost:3000

## ⚠️ Если снова зависнет:

1. Нажмите `Ctrl+C` в терминале
2. Выполните команды очистки из Шага 1
3. Попробуйте запустить снова

## 🔍 Проверка после запуска:

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform
./check-services.sh
```

## 📝 Важно:

- Запускайте сервисы в **разных терминалах**
- **Дождитесь сообщений о запуске** перед открытием ссылок
- Если зависает - остановите и запустите заново




