# 🚀 Настройка автоматического деплоя из Git

## 📋 Быстрая настройка (5 минут)

### Шаг 1: Подготовьте Git репозиторий

```bash
# Если репозиторий еще не создан
cd /Users/rakhat/Documents/webhosting/nocko-platform
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/nocko-platform.git
git push -u origin main
```

### Шаг 2: Подключите к Vercel

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите **Add New Project**
3. Выберите **Import Git Repository**
4. Выберите ваш репозиторий (GitHub/GitLab/Bitbucket)
5. Нажмите **Import**

### Шаг 3: Настройте проект

**Важно:** Укажите правильные настройки для монорепо:

- **Framework Preset**: Next.js
- **Root Directory**: `apps/website` ⚠️ **ВАЖНО!**
- **Build Command**: `npm run build` (или оставьте по умолчанию)
- **Output Directory**: `.next` (или оставьте по умолчанию)
- **Install Command**: `npm install --prefer-offline --no-audit`

### Шаг 4: Настройте переменные окружения

В **Settings** → **Environment Variables** добавьте:

```
NEXT_PUBLIC_SITE_URL=https://nocko.com
NEXT_PUBLIC_STRAPI_URL=https://your-strapi-url.com
STRAPI_API_TOKEN=your_strapi_api_token
```

**Примените для:**
- ✅ Production
- ✅ Preview
- ✅ Development

### Шаг 5: Настройте Production Branch

В **Settings** → **Git**:
- **Production Branch**: `main` (или `master`)
- **Auto-deploy**: Включено ✅

## ✅ Результат

После настройки:

1. **Каждый push в `main`** → автоматический деплой на production
2. **Каждый PR** → автоматический preview деплой
3. **Время деплоя**: 1-3 минуты
4. **Уведомления**: в Slack/Discord/Email (опционально)

## 🔍 Проверка

После первого деплоя проверьте:

1. ✅ Деплой завершился успешно
2. ✅ Сайт доступен по `https://nocko.com`
3. ✅ Все страницы загружаются
4. ✅ Изображения работают
5. ✅ API запросы к Strapi работают

## ⚡ Оптимизация скорости

### Используйте Vercel CLI для быстрого деплоя

```bash
# Локальный деплой (быстрее чем через Git)
cd apps/website
npx vercel --prod --yes
```

### Настройте уведомления

В **Settings** → **Notifications**:
- ✅ Email уведомления о деплоях
- ✅ Slack интеграция (опционально)
- ✅ Discord интеграция (опционально)

## 🆘 Решение проблем

### Деплой не запускается автоматически

1. Проверьте, что репозиторий подключен
2. Убедитесь, что Production Branch правильный
3. Проверьте права доступа к репозиторию

### Ошибка "Root Directory not found"

Убедитесь, что в Vercel Dashboard указан:
- **Root Directory**: `apps/website`

### Медленный деплой

1. Проверьте `.vercelignore` - исключены ли ненужные файлы
2. Используйте `npm ci` вместо `npm install`
3. Проверьте размер репозитория

## 📚 Дополнительно

- [Vercel Git Integration Guide](https://vercel.com/docs/concepts/git)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [Vercel Build Optimization](https://vercel.com/docs/concepts/builds/optimizing-builds)



