# 🌐 Настройка DNS для домена nocko.com на Vercel

## ✅ Возможности бесплатного плана Vercel

**Vercel Hobby (бесплатный план) поддерживает:**
- ✅ До **50 пользовательских доменов**
- ✅ Бесплатный SSL сертификат (автоматически)
- ✅ CDN и оптимизация
- ✅ Автоматические деплои из Git

## 📋 Пошаговая инструкция

### Шаг 1: Добавление домена в Vercel

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Domains**
4. Нажмите **Add Domain**
5. Введите `nocko.com`
6. Нажмите **Add**

### Шаг 2: Настройка DNS записей у регистратора домена

После добавления домена в Vercel, вы увидите инструкции по настройке DNS. Обычно требуется:

#### Вариант A: A-записи (для apex домена nocko.com)

Добавьте следующие **A-записи** у вашего регистратора домена:

| Тип | Имя (Name) | Значение (Value) | TTL |
|-----|------------|------------------|-----|
| A | `@` или пусто | `76.76.21.21` | 3600 |
| A | `@` или пусто | `76.76.22.22` | 3600 |

**Примечание:** Точные IP-адреса могут отличаться. Vercel покажет актуальные IP-адреса в панели управления после добавления домена.

#### Вариант B: CNAME (если регистратор поддерживает ALIAS/ANAME)

Некоторые регистраторы поддерживают CNAME для apex домена:

| Тип | Имя (Name) | Значение (Value) | TTL |
|-----|------------|------------------|-----|
| CNAME | `@` | `cname.vercel-dns.com` | 3600 |

#### CNAME для поддомена www

Добавьте CNAME запись для `www.nocko.com`:

| Тип | Имя (Name) | Значение (Value) | TTL |
|-----|------------|------------------|-----|
| CNAME | `www` | `cname.vercel-dns.com` | 3600 |

### Шаг 3: Проверка DNS записей

1. После добавления DNS записей, вернитесь в Vercel
2. Нажмите **Verify** рядом с доменом `nocko.com`
3. Vercel проверит правильность настроек

### Шаг 4: Ожидание распространения DNS

- ⏱️ DNS изменения обычно распространяются за **5-30 минут**
- ⏱️ В редких случаях может занять до **24 часов**
- 🔍 Проверить статус можно через [whatsmydns.net](https://www.whatsmydns.net/#A/nocko.com)

## 🔧 Примеры для популярных регистраторов

### GoDaddy

1. Войдите в панель управления
2. Перейдите в **My Products** → **DNS**
3. Добавьте записи:
   - **A Record**: `@` → `76.76.21.21`
   - **A Record**: `@` → `76.76.22.22`
   - **CNAME**: `www` → `cname.vercel-dns.com`

### Namecheap

1. Войдите в панель управления
2. Перейдите в **Domain List** → выберите `nocko.com` → **Manage** → **Advanced DNS**
3. Добавьте записи:
   - **A Record**: `@` → `76.76.21.21`
   - **A Record**: `@` → `76.76.22.22`
   - **CNAME Record**: `www` → `cname.vercel-dns.com`

### Cloudflare

1. Войдите в панель управления Cloudflare
2. Выберите домен `nocko.com`
3. Перейдите в **DNS** → **Records**
4. Добавьте записи:
   - **A**: `@` → `76.76.21.21` (Proxy: OFF)
   - **A**: `@` → `76.76.22.22` (Proxy: OFF)
   - **CNAME**: `www` → `cname.vercel-dns.com` (Proxy: OFF)

**Важно:** Отключите Cloudflare Proxy (оранжевое облако) для правильной работы Vercel.

## ✅ Проверка после настройки

После настройки DNS проверьте:

1. ✅ Домен `nocko.com` доступен
2. ✅ `www.nocko.com` редиректит на `nocko.com` (или наоборот)
3. ✅ SSL сертификат активен (автоматически через Vercel)
4. ✅ Все страницы загружаются корректно

## 🔍 Полезные команды для проверки

```bash
# Проверить DNS записи
dig nocko.com
nslookup nocko.com

# Проверить SSL сертификат
curl -I https://nocko.com
```

## 📝 Важные замечания

1. **Не удаляйте существующие DNS записи** без необходимости (например, MX записи для email)
2. **Сохраните резервную копию** текущих DNS записей перед изменениями
3. **SSL сертификат** будет автоматически выдан Vercel после успешной настройки DNS
4. **Время распространения DNS** может варьироваться в зависимости от регистратора

## 🆘 Решение проблем

### Домен не активируется

- Проверьте правильность DNS записей через [whatsmydns.net](https://www.whatsmydns.net)
- Убедитесь, что прошло достаточно времени (до 24 часов)
- Проверьте, что нет конфликтующих записей

### SSL сертификат не выдается

- Убедитесь, что DNS записи настроены правильно
- Подождите до 24 часов после настройки DNS
- Обратитесь в поддержку Vercel, если проблема сохраняется

### Сайт не загружается

- Проверьте, что проект задеплоен на Vercel
- Убедитесь, что домен правильно привязан к проекту
- Проверьте логи деплоя в Vercel Dashboard

## 📚 Дополнительные ресурсы

- [Vercel Domains Documentation](https://vercel.com/docs/concepts/projects/domains)
- [Vercel DNS Configuration](https://vercel.com/docs/concepts/projects/domains/add-a-domain)
- [Vercel Support](https://vercel.com/support)


