# ✅ Сервисы запущены!

## 🚀 Статус:

Сервисы запускаются в фоновом режиме. Подождите 30-40 секунд для полного запуска.

## 🔍 Проверка статуса:

```bash
cd /Users/rakhat/Documents/webhosting/nocko-platform
./check-services.sh
```

Или проверьте вручную:

```bash
# Проверить процессы
ps aux | grep -E "strapi|next" | grep -v grep

# Проверить порты
lsof -i :1337
lsof -i :3000

# Проверить HTTP
curl http://localhost:1337/api
curl http://localhost:3000
```

## 🌐 Открыть ссылки:

```bash
# Автоматически
./open-links.sh

# Или вручную
open http://localhost:1337/admin
open http://localhost:3000
```

## 📝 Логи:

```bash
# Strapi
tail -f logs/strapi.log

# Next.js
tail -f logs/nextjs.log
```

## 🛑 Остановить сервисы:

```bash
pkill -f "strapi develop"
pkill -f "next dev"
```

## ⏱️ Время запуска:

- **Strapi:** ~15-20 секунд
- **Next.js:** ~10-15 секунд

Подождите минуту и проверьте статус!





